# Base URL of the backend API (no trailing slash)
NEXT_PUBLIC_API_URL="https://your-backend.example.com"

# Id of the single seeded Location (MVP has one); new members are created under it
NEXT_PUBLIC_DEFAULT_LOCATION_ID="your-seeded-location-id"
